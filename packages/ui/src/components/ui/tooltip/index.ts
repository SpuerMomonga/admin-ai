import type { Tooltip as TooltipPrimitive } from 'bits-ui'
import Provider from './tooltip-provider.svelte'
import Root from './tooltip.svelte'

export {
  Root as Tooltip,
  type TooltipPrimitive,
  Provider as TooltipProvider,
}
