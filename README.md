# admin-ai

Monorepo starter for an admin system:

- `apps/web`: SvelteKit frontend with `adapter-static`
- `apps/api`: Fastify + TypeScript + Zod backend
- `packages/ui`: shared Svelte source components based on shadcn-svelte
- `packages/contracts`: shared API contracts and schemas
